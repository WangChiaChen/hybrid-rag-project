[pytest]
testpaths = tests
# src 不是套件，模組之間靠「src 在 sys.path 上」互相 import（見 tests/conftest.py）
pythonpath = src
addopts = -q
# starlette 的 TestClient 對 httpx 版本的過渡警告，跟本專案的程式碼無關
filterwarnings =
    ignore::DeprecationWarning:starlette.*
    ignore:Using `httpx` with `starlette.testclient`.*
